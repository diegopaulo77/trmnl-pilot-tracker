name: Update TRMNL Schedule
on:
  workflow_dispatch:

jobs:
  update-schedule:
    runs-on: ubuntu-latest
    steps:
      - name: Debug Google Drive Access
        uses: actions/github-script@v7
        env:
          GOOGLE_CREDENTIALS: ${{ secrets.GOOGLE_CREDENTIALS }}
          TRMNL_WEBHOOK_SCHEDULE: ${{ secrets.TRMNL_WEBHOOK_SCHEDULE }}
        with:
          script: |
            const creds = JSON.parse(process.env.GOOGLE_CREDENTIALS);
            const jwtHeader = Buffer.from(JSON.stringify({ alg: "RS256", typ: "JWT" })).toString("base64url");
            const now = Math.floor(Date.now() / 1000);
            const jwtClaim = Buffer.from(JSON.stringify({
              iss: creds.client_email,
              scope: "https://www.googleapis.com/auth/drive.readonly",
              aud: "https://oauth2.googleapis.com/token",
              exp: now + 3600,
              iat: now
            })).toString("base64url");

            const crypto = require("crypto");
            const sign = crypto.createSign("RSA-SHA256");
            sign.update(`${jwtHeader}.${jwtClaim}`);
            const signature = sign.sign(creds.private_key, "base64url");
            const jwt = `${jwtHeader}.${jwtClaim}.${signature}`;

            const tokenRes = await fetch("https://oauth2.googleapis.com/token", {
              method: "POST",
              headers: { "Content-Type": "application/x-www-form-urlencoded" },
              body: `grant_type=urn%3Aietf%3Aparams%3Aoauth%3Agrant-type%3Ajwt-bearer&assertion=${jwt}`
            });
            const tokenData = await tokenRes.json();
            const accessToken = tokenData.access_token;

            console.log("--- DEBUG START ---");
            const folderId = '17Ry-JFX2xdDM7TJNArCQ26lbbDW2JEwf';
            
            // This search asks for EVERYTHING in that folder, not just PDFs
            const searchRes = await fetch(
              `https://www.googleapis.com/drive/v3/files?q='${folderId}'+in+parents+and+trashed%3Dfalse&fields=files(id,name,mimeType)`,
              { headers: { Authorization: `Bearer ${accessToken}` } }
            );
            const searchData = await searchRes.json();
            
            if (searchData.files && searchData.files.length > 0) {
              console.log(`Found ${searchData.files.length} files in folder:`);
              searchData.files.forEach(f => console.log(`- Name: ${f.name} | Type: ${f.mimeType}`));
            } else {
              console.log("The folder appears completely empty to this script. Check Sharing permissions!");
            }
            console.log("--- DEBUG END ---");
